/**
   * ordernar:
   *  - maior para menor
   *  - menor para maior
   * filtrar:
   *  - vogais
   *  - consoantes
   *  - maiusculas
   *  - minusculas
   *  - minusculas + vogais
   *  - maisuculas + consoantes
   */
 const letters = [
    'y', 'b', 'z', 'D', 'e', 'f', 'g', 'h', 'i', 'j',
    'w', 'm', 'N', 'o', 'P', 'q', 'r', 's', 't', 'u',
    'v', 'x', 'c', 'K', 'l', 'a',
  ];

  letters.map((item, index) => console.log(`${item} ${index}`))